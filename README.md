## API REST NODE + TYPESCRIPT

### Reconstruir los modulos de no

```
npm install
```

### Renombrar el archivo .env-template a .env y asignar las variables de entorno

### Levantar la base de datos en docker 
```
docker compose up -d
```